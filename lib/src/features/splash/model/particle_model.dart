import 'dart:ui';

class Particle {
  final Offset position;
  final Color color;
  final double size;
  final double speed;
  Particle({
    required this.position,
    required this.color,
    required this.size,
    required this.speed,
  });
}
